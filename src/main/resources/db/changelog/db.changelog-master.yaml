databaseChangeLog:
    - changeSet:
          id: 1
          author: Andrey
          changes:
              - addDefaultValue:
                    schemaName: new_schema
                    tableName: person
                    columnName: id
                    defaultValueComputed: gen_random_uuid()
              - addPrimaryKey:
                    schemaName: new_schema
                    tableName: person
                    columnNames: id

              - addDefaultValue:
